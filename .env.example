# Bifrost Environment Variables Template
NODE_ENV=production
# =============================================================================
# Next.js Client-Side Variables (NEXT_PUBLIC_*)
# =============================================================================
# ⚠️  WARNING: These variables are BAKED INTO the JavaScript bundle at build time
# ⚠️  They are VISIBLE in the browser and cannot be treated as secrets
# ⚠️  Only use NEXT_PUBLIC_* for truly public configuration values
#
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key-here

# =============================================================================
# Server-Side Only Variables (NO NEXT_PUBLIC_ prefix)
# =============================================================================
API_URL=http://api:8080
SUPABASE_URL=https://your-project-id.supabase.co
