name: Bifrost Web Build and Test

on:
  push:
    branches: [ "main" ]
    paths:
      - 'src/web/bifrost-app/**'
      - '.github/workflows/nextjs.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'src/web/bifrost-app/**'
      - '.github/workflows/nextjs.yml'

defaults:
  run:
    working-directory: src/web/bifrost-app

jobs:
  build:
    name: Build Next.js Application
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: ['20.x']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: src/web/bifrost-app/package-lock.json

    - name: Cache Next.js build outputs
      uses: actions/cache@v4
      with:
        path: |
          src/web/bifrost-app/.next/cache
        key: ${{ runner.os }}-nextjs-${{ hashFiles('src/web/bifrost-app/package-lock.json') }}-${{ hashFiles('src/web/bifrost-app/**/*.js', 'src/web/bifrost-app/**/*.jsx', 'src/web/bifrost-app/**/*.ts', 'src/web/bifrost-app/**/*.tsx') }}
        restore-keys: |
          ${{ runner.os }}-nextjs-${{ hashFiles('src/web/bifrost-app/package-lock.json') }}-

    - name: Install dependencies
      run: npm ci

    - name: Run ESLint
      run: npm run lint

    - name: Build Next.js application
      run: npm run build
      env:
        NODE_ENV: production

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nextjs-build
        path: |
          src/web/bifrost-app/.next/
          src/web/bifrost-app/public/
        retention-days: 1

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: build

    strategy:
      matrix:
        node-version: ['20.x']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: src/web/bifrost-app/package-lock.json

    - name: Install dependencies
      run: npm ci

    - name: Run tests with coverage
      run: npm test -- --ci --coverage --maxWorkers=2
      env:
        NODE_ENV: test

    - name: Upload coverage reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-reports
        path: |
          src/web/bifrost-app/coverage/
        retention-days: 30

    - name: Upload coverage to workflow artifacts (Cobertura)
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-cobertura
        path: src/web/bifrost-app/coverage/cobertura-coverage.xml
        retention-days: 30

    - name: Upload coverage to workflow artifacts (LCOV)
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-lcov
        path: src/web/bifrost-app/coverage/lcov.info
        retention-days: 30

  type-check:
    name: TypeScript Type Check
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: ['20.x']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: src/web/bifrost-app/package-lock.json

    - name: Install dependencies
      run: npm ci

    - name: Run TypeScript compiler
      run: npx tsc --noEmit

  summary:
    name: Build & Test Summary
    runs-on: ubuntu-latest
    needs: [build, test, type-check]
    if: always()

    steps:
    - name: Check job status
      run: |
        echo "## 🎯 Next.js CI/CD Pipeline Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
        echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
        echo "| Build | ${{ needs.build.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Tests | ${{ needs.test.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Type Check | ${{ needs.type-check.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [[ "${{ needs.build.result }}" == "success" && "${{ needs.test.result }}" == "success" && "${{ needs.type-check.result }}" == "success" ]]; then
          echo "### ✅ All checks passed successfully!" >> $GITHUB_STEP_SUMMARY
          exit 0
        else
          echo "### ❌ Some checks failed. Please review the logs above." >> $GITHUB_STEP_SUMMARY
          exit 1
        fi
